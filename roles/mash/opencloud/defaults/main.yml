# SPDX-FileCopyrightText: 2023, 2024 Nikita Chernyi
# SPDX-FileCopyrightText: 2023-2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 Julian-Samuel Gebühr
# SPDX-FileCopyrightText: 2024 Sergio Durigan Junior
# SPDX-FileCopyrightText: 2024 noah
# SPDX-FileCopyrightText: 2025 MASH project contributors
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
# Project source code URL: https://github.com/opencloud-eu/opencloud

opencloud_enabled: false

opencloud_identifier: opencloud
opencloud_base_path: "/{{ opencloud_identifier }}"
opencloud_data_path: "{{ opencloud_base_path }}/data"

# renovate: datasource=docker depName=opencloudeu/opencloud versioning=semver
opencloud_version: 7.2.4

opencloud_uid: ""
opencloud_gid: ""

# The scheme used for serving OpenCloud.
opencloud_scheme: https

# The hostname at which OpenCloud is served.
opencloud_hostname: ""

# The path at which OpenCloud is served.
# OpenCloud requires the root path `/`.
opencloud_path_prefix: /

opencloud_container_image_repository: opencloudeu/opencloud
opencloud_container_image: "{{ opencloud_container_image_registry_prefix }}{{ opencloud_container_image_repository }}:{{ opencloud_container_image_tag }}"
opencloud_container_image_tag: "{{ opencloud_version }}"
opencloud_container_image_registry_prefix: "{{ opencloud_container_image_registry_prefix_upstream }}"
opencloud_container_image_registry_prefix_upstream: "{{ opencloud_container_image_registry_prefix_upstream_default }}"
opencloud_container_image_registry_prefix_upstream_default: docker.io/

# The base container network. It will be auto-created by this role if it doesn't exist already.
opencloud_container_network: "{{ opencloud_identifier }}"

# Control if the container network is deleted on uninstalling.
opencloud_container_network_deletion_enabled: true

# Specify how the container publishes its HTTP port (as defined by `opencloud_container_http_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:2586"), or empty string to not expose.
opencloud_container_http_host_bind_port: ""

# The port number in the container
opencloud_container_http_port: 9200

# A list of additional container networks that the container would be connected to.
# The role does not create these networks, so make sure they already exist.
# Use this to expose this container to another reverse proxy, which runs in a different container network.
opencloud_container_additional_networks: "{{ opencloud_container_additional_networks_auto + opencloud_container_additional_networks_custom }}"
opencloud_container_additional_networks_auto: []
opencloud_container_additional_networks_custom: []

# A list of additional "volumes" to mount in the container.
# This list gets populated dynamically at runtime. You can provide a different default value,
# if you wish to mount your own files into the container.
# Contains definition objects like this: `{"type": "bind", "src": "/outside", "dst": "/inside", "options": "readonly"}.
# See the `--mount` documentation for the `docker run` command.
opencloud_container_additional_volumes: "{{ opencloud_container_additional_volumes_auto + opencloud_container_additional_volumes_custom }}"
opencloud_container_additional_volumes_auto: []
opencloud_container_additional_volumes_custom: []

# opencloud_container_labels_traefik_enabled controls whether labels to assist a Traefik reverse-proxy will be attached to the container.
# See `../templates/labels.j2` for details.
#
# To inject your own other container labels, see `opencloud_container_labels_additional_labels_custom`.
opencloud_container_labels_traefik_enabled: true
opencloud_container_labels_traefik_docker_network: "{{ opencloud_container_network }}"
opencloud_container_labels_traefik_hostname: "{{ opencloud_hostname }}"
# The path prefix must either be `/` or not end with a slash (e.g. `/opencloud`).
opencloud_container_labels_traefik_path_prefix: "{{ opencloud_path_prefix }}"
opencloud_container_labels_traefik_rule: "Host(`{{ opencloud_container_labels_traefik_hostname }}`)"
opencloud_container_labels_traefik_priority: 0
opencloud_container_labels_traefik_entrypoints: web-secure
opencloud_container_labels_traefik_tls: "{{ opencloud_container_labels_traefik_entrypoints != 'web' }}"
opencloud_container_labels_traefik_tls_certResolver: default  # noqa var-naming
opencloud_container_labels_traefik_middleware_basic_auth_enabled: false
# See: https://doc.traefik.io/traefik/reference/routing-configuration/http/middlewares/basicauth/
opencloud_container_labels_traefik_middleware_basic_auth_users: ""

# Controls whether a compression middleware will be injected into the middlewares list.
# This compression middleware is supposed to be defined elsewhere (using labels or a File provider, etc.) and is merely referenced by this router.
opencloud_container_labels_traefik_compression_middleware_enabled: false
opencloud_container_labels_traefik_compression_middleware_name: ""

# Controls which additional headers to attach to all HTTP requests.
# To add your own custom request headers, use `opencloud_container_labels_traefik_additional_request_headers_custom`
opencloud_container_labels_traefik_additional_request_headers: "{{ opencloud_container_labels_traefik_additional_request_headers_auto | combine(opencloud_container_labels_traefik_additional_request_headers_custom) }}"
opencloud_container_labels_traefik_additional_request_headers_auto: {}
opencloud_container_labels_traefik_additional_request_headers_custom: {}

# Controls which additional headers to attach to all HTTP responses.
# To add your own custom response headers, use `opencloud_container_labels_traefik_additional_response_headers_custom`
opencloud_container_labels_traefik_additional_response_headers: "{{ opencloud_container_labels_traefik_additional_response_headers_auto | combine(opencloud_container_labels_traefik_additional_response_headers_custom) }}"
opencloud_container_labels_traefik_additional_response_headers_auto: |
  {{
    {}
    | combine ({'X-XSS-Protection': opencloud_http_header_xss_protection} if opencloud_http_header_xss_protection else {})
    | combine ({'X-Content-Type-Options': opencloud_http_header_content_type_options} if opencloud_http_header_content_type_options else {})
    | combine ({'Content-Security-Policy': opencloud_http_header_content_security_policy} if opencloud_http_header_content_security_policy else {})
    | combine ({'Permissions-Policy': opencloud_http_header_permissions_policy} if opencloud_http_header_permissions_policy else {})
    | combine ({'Strict-Transport-Security': opencloud_http_header_strict_transport_security} if opencloud_http_header_strict_transport_security and opencloud_container_labels_traefik_tls else {})
  }}
opencloud_container_labels_traefik_additional_response_headers_custom: {}

# opencloud_container_labels_additional_labels contains a multiline string with additional labels to add to the container label file.
# See `../templates/labels.j2` for details.
# To add your own custom labels, use `opencloud_container_labels_additional_labels_custom`
#
# Example:
# opencloud_container_labels_additional_labels_custom:
#   - my.label=1
#   - another.label="here"
opencloud_container_labels_additional_labels: "{{ opencloud_container_labels_additional_labels_auto + opencloud_container_labels_additional_labels_custom }}"
opencloud_container_labels_additional_labels_auto: []
opencloud_container_labels_additional_labels_custom: []

# A list of extra arguments to pass to the container (`docker create` command)
opencloud_container_extra_arguments: "{{ opencloud_container_extra_arguments_auto + opencloud_container_extra_arguments_custom }}"
opencloud_container_extra_arguments_auto: []
opencloud_container_extra_arguments_custom: []

# Specifies the value of the `X-XSS-Protection` header
# Stops pages from loading when they detect reflected cross-site scripting (XSS) attacks.
#
# Learn more about it is here:
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-XSS-Protection
# - https://portswigger.net/web-security/cross-site-scripting/reflected
opencloud_http_header_xss_protection: "1; mode=block"

# Specifies the value of the `X-Content-Type-Options` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Content-Type-Options
opencloud_http_header_content_type_options: nosniff

# Specifies the value of the `Content-Security-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy
opencloud_http_header_content_security_policy: ""

# Specifies the value of the `Permissions-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Permissions-Policy
opencloud_http_header_permissions_policy: "{{ 'interest-cohort=()' if opencloud_floc_optout_enabled else '' }}"

# Specifies the value of the `Strict-Transport-Security` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
opencloud_http_header_strict_transport_security: "max-age=31536000; includeSubDomains{{ '; preload' if opencloud_hsts_preload_enabled else '' }}"

# Controls whether to send a "Permissions-Policy interest-cohort=();" header along with all responses
#
# Learn more about what it is here:
# - https://www.eff.org/deeplinks/2021/03/googles-floc-terrible-idea
# - https://paramdeo.com/blog/opting-your-website-out-of-googles-floc-network
# - https://amifloced.org/
#
# Of course, a better solution is to just stop using browsers (like Chrome), which participate in such tracking practices.
# See: `opencloud_http_header_permissions_policy`
opencloud_floc_optout_enabled: true

# Controls if HSTS preloading is enabled
#
# In its strongest and recommended form, the [HSTS policy](https://www.chromium.org/hsts) includes all subdomains, and
# indicates a willingness to be "preloaded" into browsers:
# `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
# For more information visit:
# - https://en.wikipedia.org/wiki/HTTP_Strict_Transport_Security
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
# - https://hstspreload.org/#opt-in
# See: `opencloud_http_header_strict_transport_security`
opencloud_hsts_preload_enabled: false

# List of systemd services that the OpenCloud systemd service depends on
opencloud_systemd_required_services_list: "{{ opencloud_systemd_required_services_list_default + opencloud_systemd_required_services_list_auto + opencloud_systemd_required_services_list_custom }}"
opencloud_systemd_required_services_list_default: "{{ [devture_systemd_docker_base_docker_service_name] if devture_systemd_docker_base_docker_service_name else [] }}"
opencloud_systemd_required_services_list_auto: []
opencloud_systemd_required_services_list_custom: []

# List of systemd services that the OpenCloud systemd service wants
opencloud_systemd_wanted_services_list: "{{ opencloud_systemd_wanted_services_list_default + opencloud_systemd_wanted_services_list_auto + opencloud_systemd_wanted_services_list_custom }}"
opencloud_systemd_wanted_services_list_default: []
opencloud_systemd_wanted_services_list_auto: []
opencloud_systemd_wanted_services_list_custom: []

# Controls how long to wait for the container to stop gracefully before killing it.
opencloud_container_stop_grace_time_seconds: "{{ devture_systemd_docker_base_container_stop_grace_time_seconds }}"

# Controls how long the pre-start cleanup should wait for stale container-name release
# before failing startup explicitly.
opencloud_container_cleanup_timeout_seconds: "{{ devture_systemd_docker_base_container_cleanup_timeout_seconds }}"

# Controls the delay between pre-start cleanup retries.
opencloud_container_cleanup_retry_delay_seconds: "{{ devture_systemd_docker_base_container_cleanup_retry_delay_seconds }}"

# Shell command used in the systemd pre-start phase to deterministically remove a stale
# container and wait until Docker releases its name.
opencloud_container_cleanup_command: |
  i=0;
  while [ "$i" -lt {{ opencloud_container_cleanup_timeout_seconds }} ]; do
  {{ devture_systemd_docker_base_host_command_docker }} stop -t {{ opencloud_container_stop_grace_time_seconds }} {{ opencloud_identifier }} >/dev/null 2>&1 || true;
  {{ devture_systemd_docker_base_host_command_docker }} rm -f {{ opencloud_identifier }} >/dev/null 2>&1 || true;
  if {{ devture_systemd_docker_base_host_command_docker }} container inspect {{ opencloud_identifier }} >/dev/null 2>&1; then
  i=$((i + 1));
  sleep {{ opencloud_container_cleanup_retry_delay_seconds }};
  continue;
  fi;
  if {{ devture_systemd_docker_base_host_command_docker }} info >/dev/null 2>&1; then
  exit 0;
  fi;
  echo "Failed to query Docker daemon while cleaning up {{ opencloud_identifier }}" >&2;
  exit 1;
  done;
  echo "Failed to remove stale container {{ opencloud_identifier }} after {{ opencloud_container_cleanup_timeout_seconds }} attempts" >&2;
  exit 1

# The generated opencloud.yaml contains persistent instance secrets; never template it.
opencloud_config_path: "{{ opencloud_base_path }}/config"
# Fail before creating directories if this optional mount point is not mounted.
opencloud_required_mount_path: ""
opencloud_url: "{{ opencloud_scheme }}://{{ opencloud_hostname }}"
opencloud_admin_password: ""
opencloud_default_language: ru
opencloud_log_level: info
opencloud_restart_necessary: false

# The built-in collaboration service serves WOPI through the main proxy.
opencloud_collaboration_enabled: false
opencloud_collaboration_app_name: EuroOffice
opencloud_collaboration_app_product: OnlyOffice
opencloud_collaboration_app_url: ""
# Matches the official EuroOffice integration; independent of TLS validation.
opencloud_collaboration_app_proof_disable: true

opencloud_search_enabled: false
opencloud_search_tika_url: ""

# Set to the upstream search schema generation when an upgrade requires a rescan.
# Keep it unchanged for patch releases; completion is persisted outside the index.
opencloud_search_index_generation: ""
opencloud_migrations_path: "{{ opencloud_base_path }}/migrations"
opencloud_search_reindex_timeout: 86400
opencloud_search_reindex_concurrency: 3
# The internal gRPC endpoint uses plaintext by default; this does not affect HTTPS.
opencloud_search_reindex_insecure: true

opencloud_smtp_enabled: false
opencloud_smtp_host: ""
opencloud_smtp_port: 8025
opencloud_smtp_sender: ""
opencloud_smtp_username: ""
opencloud_smtp_password: ""
opencloud_smtp_authentication: none
opencloud_smtp_encryption: none

# Additional KEY=value lines. Keep secrets in encrypted inventory.
opencloud_environment_variables_additional_variables: ""
